export type Color = {
  bgColor: string;
  circleColor: string;
  navColor: string;
  accentColor: string;
  menuColor:string;
};

// progressColor
// calendarCellColor
// calendarCircleColor
// calendarActiveCellColor
// calendarTodayColor
